# KnowFlow 评测集 v1

`dataset.json` 是首版可重复的中文企业知识样本：20 篇文档、50 条检索问题和 10 条问答案例。每条检索问题通过 `relevant` 标注文档 ID；问答案例通过 `citations` 标注应引用的文档。文档正文使用当前 Markdown 解析规则，实际运行评测时应上传这些正文后再按返回的文档和分块位置记录原始结果。

当前数据集只提供标注，不代表任何召回率、排序或问答质量结论。评测脚本完成批量上传和真实服务运行后，须保存逐条响应、模型配置、重排状态及汇总指标；协议替身结果不能用于质量结论。
