# 后续实施进度

依据用户批准的 18 项计划持续推进；每个增量独立验证，不将局部完成当作整个项目完成。

| 项目 | 状态 | 验收证据或缺口 |
|---|---|---|
| 1 权限向量检索 | 已完成 | 搜索接口、Qdrant 前置过滤、MySQL 最终授权；91 项完整测试通过，含撤权和跨库候选 |
| 2 真实 Embedding 联调 | 已完成 | 百炼 text-embedding-v4 1024 维；真实上传到搜索链路通过，见 live-validation.md；未做质量评测 |
| 3 Swagger 与演示 | 已完成 | OpenAPI、Bearer 授权及演示已实现；配置 OpenAPI 3.0 消除默认 3.1 转换警告，补充参数及响应类型契约；103 项完整测试通过 |
| 4 文档管理与重建 | 已完成 | 列表、详情、版本化重建、删除与可重试向量清理；102 项完整测试通过，含删除竞态、历史集合清理、故障恢复及事务回滚；原文件和历史分块保留待对账 |
| 5 BM25 | 已完成 | Lucene 10.3.2、CJKAnalyzer、单 Worker 写入、API 已提交 Reader、V10 独立进度及 BM25 模式；11 项专项、114 项完整回归通过，含开发配置下的测试目录隔离、版本切换、删除及重建 |
| 6 Hybrid 与 RRF | 已完成 | HYBRID 模式、两路分别授权后 k=60 RRF、去重及稳定排序；29 项专项、120 项完整回归通过，含跨库伪造、撤权、未就绪进度和故障拒绝降级 |
| 7 Rerank | 已完成 | 百炼原生重排客户端、双开关、候选上限、总超时 / 响应大小限制、失败回退及最终授权；138 项完整回归通过；真实三文档检索返回 APPLIED，见 live-model-check.md；不代表质量评测 |
| 8 证据问答 | 已完成 | Spring AI 同步 JSON 回答、服务端引用与连续摘录校验、生成后全部输入证据重新授权、实际 usage；本地与百炼 Compose 端到端验证通过 |
| 9 SSE | 已完成 | 真实模型流订阅、六类事件、部分输出与完成状态、逐次权限复核、断线与总期限取消；Compose 百炼流式演示已收到 metadata/citation/done |
| 10 会话持久化 | 已实现并通过自动测试 | V11 三张表、同步 / SSE 终态保存、本人会话分页、全部来源重新授权、并发续问与中断恢复；167 项完整回归通过 |
| 11 查询改写 | 已实现并通过自动测试 | rewrite 显式开关、最近有效问答、V12 记录、128 Token / 3 秒 / 无重试、失败回退、跨轮来源复核；178 项回归通过；真实追问改写 APPLIED，对照排名均为 1，未显示收益，见 live-model-check.md |
| 12 模型韧性 | 已实现并通过自动测试 | 连接 / 首正文 / 空闲 / 总期限、0–2 次瞬态重试、可选备用聊天模型一次切换、正文后禁止重试；190 项完整回归通过；故障注入与真实 qwen-turbo 备用调用组合验证通过，正文后不切换，见 live-model-check.md |
| 13 Redis 缓存 | 已完成 | 知识库与任务版本缓存、提交后失效、终态数据库读取、故障回源；203 项完整回归通过，本地已启用 |
| 14 限流和幂等 | 已完成 | 原子限流、上传/重新索引 Redis 协调、令牌释放与数据库兜底；219 项完整回归通过 |
| 15 统计观测 | 已完成 | 四类调用账本、价格快照、标价估算、20 元预算预占/结算/拒绝、requestId 追踪及管理员查询；预算专项 15 项通过 |
| 16 对账恢复 | 部分完成 | 已有数据库只读计数；恢复已限定任务 ID、默认预览并按阶段计数。已增加按知识库分页的文件摘要、Qdrant 分块、Lucene 提交核验、直属存储目录孤立项、指定集合孤立向量和集合登记扫描，见 external-reconciliation.md；超上限游标与完整恢复演练待完成 |
| 17 完整 Compose | 已实现并完成本地验收 | Vue/Nginx、API、Worker 与基础服务一键启动；独立空卷与重启恢复通过，真实百炼链路和生产页面已验证，见 compose-validation.md |
| 18 评测与简历材料 | 部分完成 | `scripts/evaluate.ps1` 已提供 Vector/BM25/Hybrid 的 Recall@5、MRR、nDCG@5 和原始 JSON；当前仅 1 条演示样本，不能作为质量结论，Rerank 待真实启用后评测 |

前端进度：Vue 3 + TypeScript + Vite 已完成登录/注册、JWT 会话、知识库创建与选择、Hybrid 搜索、引用展示、文档上传/重处理/删除、任务轮询、文档列表、会话历史、SSE 问答、流式取消和成员管理；`vue-tsc --noEmit && vite build` 通过，Vite 开发服务器 HTTP 200。关键交互回归及生产 Nginx 部署验收已完成，见 compose-validation.md。

后续执行约束：新增真实模型联调与评测累计上限为 20 元，预算保护与调用统计已实现；每批先核验余额和费用上界，额度不足时停止付费调用。真实问答与 SSE 已有联调记录，真实重排、改写和备用调用已有联调记录；改写收益和检索质量仍待正式评测；日常协议测试不代表真实效果。Vue 前端已重写，生产部署已验证，后续完成外部索引对账、评测与项目交付；不增加 OCR、多机 Lucene 或微服务拆分。下方按日期保留历史记录，其中“下一步”和“待实现”描述以本页顶部当前状态为准。

GitHub 同步：每个可运行增量通过完整回归后，提交并推送到已有 origin/master，再核对远端提交。保留现有历史，不强制推送；密钥、本地数据、构建和测试产物不上传。首次同步包含此前尚未提交的问答、模型韧性及缓存增量。

2026-09-04 测试隔离修复：VectorTests 明确关闭生产模型预算保护，避免 worker/api 测试配置中的替身模型被真实价格目录拦截；JDK 25 完整 `verify` 通过 247 项测试（0 失败、0 错误），提交 `1d58421` 已推送 `origin/master`。

2026-09-04 预算前置核验与批次修复：官方文档确认 text-embedding-v4 兼容接口每批最多 10 条，原 Worker 的 16 条批次在加入真实上限规则的协议替身上复现失败（target/embedding-limit-repro.log）。改为每批 10 条，17 个分块按 10+7 入库并验证完整激活、缓存失效与重放幂等。JDK 25 完整 verify 245 项通过（0 失败、0 跳过），日志 target/embedding-limit-build.log。无新表、依赖或付费调用。已记录模型上界和 MySQL 预算事务方案（model-budget-design.md），预算拦截仍待实现，不将方案视为已启用的保护。

2026-09-04 预算、追踪与 Compose：V18 增加共享 20 元预算行及调用预留/结算字段，V19 保存 requestId；预算专项 15 项通过。Dockerfile 多阶段构建 JDK 25 镜像，Compose 增加 API/Worker 和共享数据卷；`docker compose config`、镜像构建、保留数据启动及 `/actuator/health` 均通过。Compose 演示脚本使用百炼 text-embedding-v4 完成注册、建库、上传、Kafka Worker 入库和同模型检索，数据库记录实际估算费用 0.000061 CNY，预算预留已结算为 0；未调用聊天模型。只读对账接口已增加，完整回归待 Docker 测试环境稳定后重跑。

2026-09-04 价格快照与估算增量：V17 增加版本价格目录与逐次调用快照，按服务地址/模型匹配北京公开标价；未知价格、用量不足、模型名变化或超定价范围保持 NULL。四类调用传入自身服务地址，终态通过 DECIMAL 计算标价估算，失败的已知消耗仍保留。专项 13 项及完整 verify 245 项通过（0 失败、0 跳过），日志 target/model-price-build.log，覆盖极小金额、未来价格、快照隔离和未知费用汇总。打包 API 健康 UP、V17 迁移和费用 Swagger 字段通过，临时进程已停止。没有付费模型调用。下一增量为累计 20 元预算预留与拦截，完成前继续暂停真实联调。

2026-09-04 Rerank 记账增量：重排调用接入同一账本，记录总量而不伪造输入/输出分布，非法结果的已知用量仍保留。专项 19 项通过，覆盖真实 MySQL、未知/非法/零用量、超时和账本故障；搜索集成及管理员总量汇总断言通过。JDK 25 完整 verify 237 项通过（0 失败、0 跳过），日志 target/rerank-ledger-build.log；打包 API 健康 UP、Swagger 四类调用说明验证通过，临时进程已停止。没有新增表、依赖或付费请求。已核对并保存官方北京公开价格，见 model-pricing-research.md；尚未启用成本估算，下一增量实现版本价格和预算。

2026-09-04 Embedding 记账增量：查询和 Worker 两处共用 EmbeddingCalls，使用 embedForResponse 保留原生 usage；V16 增加 task_id，QUERY / INDEX 分开记录。扩展分批、失败、维度错误和重试预算断言，并增加账本不可用时阻止调用及恢复成功的用例；修复该故障用例遗留待重试任务对后续测试的干扰。JDK 25 完整 verify 228 项通过（0 失败、0 跳过），日志 target/embedding-ledger-final-build.log。打包 API 健康 UP、V16 迁移、Swagger taskId 字段验证通过，临时进程已停止。没有真实模型调用。下一增量为 Rerank 记账，随后补齐价格、预算与请求追踪。

2026-09-04 改写记账增量：复用 V15 model_call，增加 REWRITE 类型并关联当前消息；跳过改写不记调用，内容非法仍保留已完成调用与实际 usage。扩展原有成功、失败、非法内容和无历史测试，JDK 25 完整 verify 227 项通过（0 失败、0 跳过），日志 target/rewrite-ledger-build.log。更新 Swagger 说明后重新打包，API 健康 UP、说明验证通过，临时进程已停止。没有新增表、依赖或真实模型调用。下一增量接入查询和 Worker 的 Embedding 用量。

2026-09-04 聊天记账增量：JDK 25 完整 verify 通过 227 项（0 失败、0 跳过），记录在 target/model-call-final-build.log。新增真实 MySQL 账本与管理员 HTTP 验证，原有本地主备协议测试纳入真实记账；同步及 SSE 验证消息关联。发现 Spring AI 2.0.1 将缺失流式 usage 合成全零，账本与 SSE 现共用保守判定，未知不记录为零。打包 API 健康 UP、V15 迁移及两个管理员 Swagger 路径通过，临时进程已停止。没有付费模型调用；第 15 项仍需其余模型链路、版本价格、预算与追踪。

现有基础模块上次通过 88 项测试。Qdrant 使用真实容器，模型 HTTP 测试替身仅验证协议与工程行为，不具备语义检索能力。

2026-09-04 文档删除增量：JDK 25 下完整 `verify` 通过 102 项测试（0 失败、0 跳过），记录在 `target/deletion-build.log`。打包应用以 API + Worker 配置、关闭 Embedding 启动，通过数据库健康检查和 DELETE OpenAPI 路径检查；临时应用已停止。本轮没有调用百炼。

2026-09-04 OpenAPI 修复：原打包应用默认设置复现 25 条 JsonSchema 转换警告，使用同一制品仅覆盖版本配置后为 0 条，参数边界和 multipart binary 类型保留。正式配置及新增契约测试的完整 `verify` 通过 103 项测试（0 失败、0 跳过），构建日志 `target/openapi-build.log` 中无该警告；模型仍使用本地测试替身。

2026-09-04 BM25 增量：两次完整回归均通过 114 项测试；最终 `target/bm25-final-build.log` 对应显式启用开发 BM25 配置的隔离验证，非测试索引目录没有被创建。独立 API 与 Worker 启动、V10 迁移及两篇已有演示文档的 BM25 同步通过，未调用真实模型。下一增量为 Hybrid 与 RRF。

2026-09-04 Hybrid 增量：`target/hybrid-build.log` 完整回归 120 项通过（0 失败、0 跳过），并检查启用开发 BM25 环境变量时没有访问非测试索引目录。真实 Qdrant、MySQL 和磁盘 Lucene 验证融合与授权，Embedding 仅用本地 HTTP 替身。无真实模型调用，无检索质量或性能结论。下一增量为 Rerank。

2026-09-04 Rerank 增量：`target/rerank-final-build.log` 完整回归 138 项通过（0 失败、0 跳过）。验证非法响应、慢速正文总期限、128 KiB 接收上限、关闭 / 跳过 / 回退状态，以及调用期间的撤权、删除和版本切换。仅一次百炼两短句探针返回 31 Token；本地默认关闭，密钥复用已有变量。下一增量为基于证据的问答与引用。

2026-09-04 证据问答增量：`target/answer-build.log` 完整回归 151 项通过（0 失败、0 跳过），使用 JDK 25、真实 MySQL / Kafka / Qdrant 与本地模型 HTTP 替身。验证非法 JSON、未知引用、伪造摘录、截断、无证据跳过、无引用拒答、缺少 usage、模型失败不自动重试，以及生成期间撤权、删除、版本变化和未引用输入片段变化。新接口出现在 Swagger；没有新增数据库表。本地已启用聊天与 BM25，聊天预算 512 输出 Token、关闭思考，本轮真实模型调用为零。下一增量为 SSE。

2026-09-04 SSE 增量：`target/sse-build.log` 完整回归 159 项通过（0 失败、0 跳过）。8 项 SSE 专项使用真实 HTTP 客户端、Spring AI 兼容协议替身、MySQL 与 Qdrant，证明首段正文在模型结束前送达，客户端断开与总期限触发上游 HTTP 关闭。验证正常引用结束、无证据跳过、截断、缺少结束标记、虚构编号及生成中撤权均不能产生成功 done；Swagger 暴露 text/event-stream。本轮未调用真实百炼。下一增量为会话、消息与引用持久化。

2026-09-04 会话增量：`target/conversation-build.log` 在 JDK 25 下完整回归 167 项通过（0 失败、0 跳过）。真实 MySQL 验证 V11、引用与消息事务回滚、同时续问仅一条 RUNNING、遗留恢复和终态不能被迟到回调覆盖；真实 HTTP 验证同步 / SSE 成功、错误、取消持久化，以及本人隔离、跨库拒绝、来源删除 / 换版 / 未引用片段修改后的整体隐藏。所有模型使用本地替身。本轮没有真实 API 消耗。下一增量为查询改写。

2026-09-04 改写增量：`target/rewrite-build.log` 完整回归 178 项通过（0 失败、0 跳过）。增加 11 项改写用例，覆盖指代对照、首次与失效历史跳过、异常 / 非法格式回退、3 秒超时、范围隔离、模型期间撤权，以及新答案引用另一篇有效文档但上游历史来源失效时仍整体隐藏。修复请求级选项丢失默认配置的问题，改写和 SSE 现在通过 mutate 保留原模型配置；HTTP 断言验证思考关闭、改写 128 Token、SSE 512 Token。此次没有真实模型调用，未声称真实改写收益。下一增量为模型超时、有限重试与 Fallback。

2026-09-04 模型韧性增量：`target/resilience-build.log` 在 JDK 25 下完整回归 190 项通过（0 失败、0 跳过）。12 项新增主备 HTTP 测试验证 Spring Bean 主备选择、瞬态分类和异步异常解包、精确尝试次数、真实请求选项、慢速同步响应总期限、首正文超时切换、正文后空闲超时不切换、取消关闭连接及跨模型共享总期限。默认 CHAT_RETRIES=0、备用模型关闭；改写和 Embedding 不参与聊天模型切换。本轮没有真实 API 调用。下一增量为 Redis 缓存。

2026-09-04 知识库缓存增量：JDK 25 完整 verify 通过 196 项测试（0 失败、0 跳过），记录为 target/kb-cache-build.log。15 项知识库测试使用真实 MySQL / Redis，覆盖版本隔离、权限变化、事务回滚、提交后失效、迟到回填、ACL 故障与命令超时。Compose Redis 已健康启动；打包 API 显式启用缓存且关闭模型，应用数据库迁移至 V13，健康检查 UP，匿名访问 401；验证进程已停止。默认 CACHE_ENABLED=false，配置与边界见 cache-design.md。本轮付费模型调用为零。下一增量为任务状态缓存。

2026-09-04 任务缓存增量：相关测试先通过 116 项；最终 JDK 25 完整 verify 通过 203 项（0 失败、0 跳过），日志为 target/task-cache-build.log。真实 Redis / MySQL / Kafka / Qdrant 验证空 stage、短 TTL、非法 JSON、撤权、回滚、迟到回填、Kafka 重放、解析重试、向量分批入库和租约耗尽；终态直接查数据库。缓存读写与提交后失效复用 RedisCache，V14 增加任务版本。显式 CACHE_ENABLED=true 下回归通过，测试未使用开发 Redis；本地 .env 随后启用缓存。打包 API 迁移至 V14，整体及 Redis 健康 UP、匿名任务访问 401，临时验证进程已停止。没有真实模型调用。下一增量为第 14A 项原子接口限流。

2026-09-04 接口限流增量：JDK 25 完整 verify 通过 210 项测试（0 失败、0 跳过），记录为 target/rate-limit-build.log。7 项限流专项验证真实 Redis 原子并发、过期恢复、来源头伪造、用户与路由共享、故障拒绝和超时；已有 SSE 集成用例验证完整异步生命周期只计数一次。打包 API 临时使用 1 秒 / 2 次注册额度，三个无效请求依次返回 400、400、429，Retry-After=1，健康 UP；进程已停止，临时额度不写入配置。默认限流已启用，无新增模型调用。下一增量为 Redis 请求幂等。

2026-09-04 请求幂等增量：JDK 25 完整 verify 通过 219 项测试（0 失败、0 跳过），日志为 target/idempotency-build.log。文档专项 33 项，新增真实 Redis/MySQL 并发、旧持有者释放、外层事务提交/回滚、慢上传超过租约、获取/释放 Redis 故障及同键/不同键重新索引验证。协调默认启用，30 秒租约，不续租；处理中的同键请求返回 409，完成后仍复用原任务，数据库约束和任务租约保持最终正确性。本步没有新表。打包 API 健康 UP，Swagger 已描述上传/重新索引的处理中 409，临时进程已停止；没有真实模型调用。下一增量为第 15 项调用统计、成本估算与可观测性。

2026-09-04 交付收尾增量：密码下限统一为 8 个字符，后端、前端、Swagger 和 README 同步；认证边界与文档契约测试覆盖 7 位拒绝及 8 位注册登录。恢复接口要求最多 100 个任务 ID，默认预览，按解析/向量阶段计数，执行时锁定并复核状态；覆盖租约续期、旧版本、删除、重复执行及激活版本保护。JDK 25 完整 verify 252 项通过（0 失败、0 错误、0 跳过），日志 target/delivery-final-regression.log。前端 13 项测试及生产构建通过，新增会话生命周期、重复发送、取消、迟到响应隔离和引用撤回回归。API/Worker 镜像已重建并保留数据重启，本地健康 UP，7 位注册 HTTP 400、8 位注册 HTTP 201、8 位登录 HTTP 200。外部索引真实对账、前端生产 Compose 和正式评测仍待完成。

2026-09-04 完整部署增量：新增 Vue/Nginx 镜像、SSE 无缓冲代理、API/Worker 健康检查及完整环境变量传递。隔离空库与保留数据重启均 PASS，正常项目 8088 入口完成百炼真实上传、检索、SSE 与浏览器历史引用验证。修复预算环境变量绑定，并将 Spring 测试上下文缓存限制为 2，避免测试数据库累积耗尽 Docker 内存；串行完整 verify 252 项通过（0 失败、0 错误、0 跳过），日志 target/compose-final-regression.log。前端 13 项测试通过；临时验收环境已停止，正常项目全部健康。下一步补齐真实 Rerank、查询改写对照和备用聊天模型联调。

2026-09-04 真实模型联调：重排 APPLIED；一次追问改写成功，对照排名均为 1，不声称收益；真实 qwen-turbo 备用响应 63 Token，正文后故障不切换。完整 verify 252 项与显式付费联调 1 项通过。脱敏记录和预算详情见 live-model-check.md。下一步扩展外部索引对账与限定范围恢复演练。

2026-09-04 外部对账增量：新增按知识库分页的原文件 SHA-256、Qdrant 点身份与 Lucene 已提交索引核验，并增加直属存储目录文件分类、指定集合孤立向量和集合登记扫描；保留并发变化、不可达和超上限状态。修复 Maven 普通测试继承本地备用模型配置的问题。完整 verify 260 项通过；生产入口 3 篇文档、2 页、11 项目录文件、46 项向量、集合登记及 Swagger 验收通过，无新增模型调用。文件扫描专项 6 项、真实 Qdrant 孤立向量和集合扫描专项 2 项通过，原始记录见 external-reconciliation.md。超上限游标与完整恢复演练仍待完成。

2026-09-04 容器恢复演练：新增 `scripts/recovery-drill.ps1`，使用独立 Compose 项目和本地协议替身验证 Worker、Kafka、Redis、Qdrant 暂停/恢复。任务恢复、Redis 数据库回源、Qdrant 503 与恢复后检索均通过，结果见 `target/recovery-drill-result.json` 和 `recovery-drill.md`。该演练不包含真实模型调用，也不替代生产级故障演练。下一步为正式检索 / 问答评测数据集。

2026-09-04 评测集增量：新增 `evaluation/v1/dataset.json`，包含 20 篇中文企业文档、50 条带相关文档标注的检索问题和 10 条带引用标注的问答案例，并通过数量校验。当前仅完成数据集和标注，尚未运行真实评测或生成质量指标；下一步改造脚本批量上传并保存 Vector、BM25、Hybrid、Hybrid + Rerank 的逐条结果。
