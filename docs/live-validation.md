# 百炼小样本联调记录

验证日期：2026-09-04。仅使用仓库公开演示文档和简短连接提示，没有压测或大规模索引。

## 配置

- 北京地域 OpenAI 兼容接口：`https://dashscope.aliyuncs.com/compatible-mode/v1`。
- Embedding：`text-embedding-v4`，实际返回 1024 维；API 与 Worker 使用相同配置。
- 用户指定聊天模型：`qwen3.8-flash`。已验证短调用可用，关闭思考，探针输出上限 32 Token。
- 密钥只保存于已忽略的 `.env`，本文及测试代码不含密钥。CHAT_* 是后续问答模块的配置准备，目前尚未实现产品问答接口。

## 已验证

1. Embedding 探针成功，服务返回 prompt_tokens=9、total_tokens=9。
2. 聊天探针成功，服务返回 prompt_tokens=30、completion_tokens=2、total_tokens=32，finish_reason=stop。
3. 项目 API 注册、登录、创建知识库并上传 docs/demo.md。
4. 独立 Worker 经 Kafka 接收、提取分块，调用真实模型写入 Qdrant，任务完成并激活索引。
5. 使用同一真实模型查询，返回 demo.md 的原文和段落编号。
6. scripts/demo.ps1 最终复验成功，并检查返回正文属于本次上传文档；修复了 PowerShell JSON 数组外层包装导致结果展示为空的问题。
7. Swagger JSON、UI 页面及 JavaScript 资源通过 HTTP 自动测试；完整 Maven verify 通过 92 项测试。

## 费用与结论边界

两个独立连接探针的服务端 usage 合计 41 Token。随后进行两次短文档演示联调；当前应用尚未保存这些链路调用的 usage，因此 41 不是本次所有联调的总 Token 数，不能据此推算总费用。没有编造费用、延迟分位数或检索质量指标。

第一次演示存在脚本展示问题，第二次修复后验证了正文内容。两次各创建一个独立 demo_ 账号和知识库，数据保留。临时 API 与 Worker 已停止，MySQL、Kafka、Qdrant 保持运行。后续普通测试继续使用测试替身，不使用此密钥；真实模型仅在明确的联调或评测中调用。

## 2026-09-04 BM25 本地联调

以打包制品分别启动 API 与 Worker，显式启用 BM25、关闭 Embedding；两者共享 `.data/lucene`。本地 MySQL 的 V10 迁移成功，已有两篇 READY 演示文档的 BM25 进度均变为 READY，索引版本与激活版本均为 1。API 健康检查通过，OpenAPI 枚举包含 BM25；两个临时进程随后已停止，索引保留。

此次未重新调用真实 Embedding 或聊天模型。真实 MySQL 与临时磁盘索引的自动测试另外验证了 HTTP BM25 查询、中文片段及引用、跨库过滤、撤权、提交边界、失败重试、重启及新目录重建。该联调不是检索质量评测或压测。
## 2026-09-04 Rerank 连通性

使用同一本地百炼密钥，向北京原生文本排序接口发送一次 `gte-rerank-v2` 请求，问题为“如何报销”，候选仅为“报销需要提交发票。”和“请假需要主管审批。”。HTTP 200，返回两个有效位置索引及有限相关性分数，服务 usage.total_tokens 为 **31**。脱敏记录在 `target/rerank-live-probe.json`。

该 31 Token 仅属于本次单独探针；此前演示链路的 usage 未完整记录，不能将它相加推算整个项目总费用。此次没有调用真实聊天或 Embedding 服务。重排配置已保存到忽略的 `.env`，通过已有密钥变量引用，默认关闭；应用端正常排序、超时、非法响应及撤权验证使用本地 HTTP 替身。

## 2026-09-04 同步问答本地验证

本地 `.env` 已启用 `qwen3.8-flash` 聊天模型与 BM25，聊天密钥引用已有百炼变量。每次最多 512 输出 Token、关闭思考、超时 20 秒、不自动重试；无证据不调用聊天模型。新增接口的协议、引用和授权行为通过本地 HTTP 模型替身与真实存储验证，完整回归 151 项通过。本增量没有调用真实模型，不声称已完成真实百炼 JSON 问答端到端联调或效果评测。

## 2026-09-04 SSE 本地验证

完整回归 159 项通过，8 项 SSE 专项验证模型未结束时已发送正文、客户端断开取消上游、总期限取消、截断与撤权失败状态。服务端与测试客户端之间使用真实 HTTP，模型端为本地兼容协议替身。本轮真实百炼调用为零，尚未验证百炼流式服务的端到端表现，也没有生成性能或质量结论。

## 2026-09-04 改写与请求预算验证

完整回归 178 项通过。此次发现请求级新建选项会丢失默认 Token 上限、JSON 格式和关闭思考附加参数，已改为复制默认选项后局部修改。捕获本地 HTTP 请求验证：改写 128 输出 Token、JSON 格式、关闭思考；SSE 512 输出 Token、关闭思考。此前 SSE 仅使用测试替身，未因该问题调用真实百炼。改写真实收益尚待评测，本轮真实模型调用为零。

## 2026-09-04 模型超时与主备验证

完整回归 190 项通过。新增 12 项使用本地 HTTP 主备服务与真实 Spring AI 客户端的测试，验证有限重试、首正文及空闲超时、共享总期限和连接取消。主备是测试服务，不是两家真实模型服务；本地仍未启用备用模型，没有额外百炼调用，不能据此声称真实服务可用性提升。

## 2026-09-04 Compose 真实 Embedding 演示

在 Docker Compose API/Worker 进程中执行 `scripts/demo.ps1`，完成注册、登录、建库、上传 `docs/demo.md`、Kafka 异步处理、真实 `text-embedding-v4` 向量入库和同模型向量检索。脚本返回本次文档的三个正文片段及段落编号，任务成功并激活版本。数据库账本记录两次 Embedding 调用，实际 usage 对应的估算费用合计 **0.000061 CNY**，预算预留已全部结算；本次没有调用聊天模型。该演示证明 Compose 运行链路和协议闭环，不代表检索质量或性能评测。
